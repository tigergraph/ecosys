USE GRAPH ldbc_snb
SET syntax_version = "v2"
CREATE OR REPLACE QUERY ins5(
    VERTEX<Person> personId, 
    VERTEX<Forum> forumId,
    DATETIME creationDate) {
  INSERT INTO HAS_MEMBER VALUES(forumId, personId, creationDate);
}