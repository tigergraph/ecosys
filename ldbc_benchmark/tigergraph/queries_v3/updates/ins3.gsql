USE GRAPH ldbc_snb
SET syntax_version = "v2"
CREATE OR REPLACE QUERY ins3(VERTEX<Person> personId, VERTEX<Comment> commenId, DATETIME creationDate) {
  INSERT INTO LIKES (FROM, TO, creationDate) VALUES(personId, commenId, creationDate);
}