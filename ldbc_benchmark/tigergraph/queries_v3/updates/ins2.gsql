USE GRAPH ldbc_snb
SET syntax_version = "v2"
CREATE OR REPLACE QUERY ins2(UINT personId, UINT postId, DATETIME creationDate) {
  INSERT INTO LIKES (FROM, TO, creationDate) VALUES(personId Person, postId Post, creationDate);
}