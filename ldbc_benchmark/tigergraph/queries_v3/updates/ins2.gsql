USE GRAPH ldbc_snb
SET syntax_version = "v2"
CREATE OR REPLACE QUERY ins2(VERTEX<Person> personId, VERTEX<Post> postId, DATETIME creationDate) {
  INSERT INTO LIKES (FROM, TO, creationDate) VALUES(personId, postId, creationDate);
}