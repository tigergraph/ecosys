USE GRAPH ldbc_snb
SET syntax_version = "v2"
CREATE OR REPLACE QUERY ins8(VERTEX<Person> person1Id, VERTEX<Person> person2Id, DATETIME creationDate) {
  INSERT INTO KNOWS (FROM, TO, creationDate) VALUES(person1Id, person2Id, creationDate);
}