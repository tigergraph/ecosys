DROP ALL

CREATE VERTEX Node (PRIMARY_ID id STRING, name STRING, score FLOAT, tag STRING)
CREATE DIRECTED EDGE LinkTo (FROM Node, TO Node, weight FLOAT, tag STRING) WITH REVERSE_EDGE="LinkFrom"
CREATE UNDIRECTED EDGE Link (FROM Node, TO Node, weight FLOAT, tag STRING)

CREATE GRAPH generic (*)
