use graph ldbc_snb

drop query q320_2
CREATE QUERY q320_2 () SYNTAX _v2 {
  MaxAccum<INT> @@oldest;
  //find how many post/comment liked by a person
  V = SELECT f
      FROM :f - (CONTAINER_OF>)-Post:p- (<LIKES)-Person:s
      PER (s,f)
      ACCUM @@oldest += datetime_to_epoch(s.creationDate),
      @@oldest += datetime_to_epoch(f.creationDate);

  Print @@oldest;

}

//install query q311
//run query q5()
