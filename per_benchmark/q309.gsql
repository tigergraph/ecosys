use graph ldbc_snb

drop query q309
CREATE QUERY q309 () SYNTAX _v2 {

  SumAccum<int> @forum_cnt;

  V = SELECT f
      FROM Person:s- (LIKES>)-Post:p - (<CONTAINER_OF)-:f -(HAS_TAG>)- :t
      WHERE s.firstName == "Viktor" AND s.lastName == "Akhiezer"
      PER (s,f)
      ACCUM s.@forum_cnt += 1, f.@forum_cnt += 1;

  Print V.size();

}

//install query q308a
//run query q308a()
