---
# @           : this file
# {x}         : the directory of file x
# &x          : any file which matches pattern x
# **         : any depth of arbitrary directories
# *           : wildcard character
# [x|y...|z]  : file/directory x, y, or z

  algorithm:
    name: Cycle Detection (Full Result)
    filename: "tg_cycle_detection.gsql"
    sha_id: ed6ea869749977cc0f3df71225d7325fb81c9767
    description: The Cycle Detection problem seeks to find all the cycles (loops) in a graph. We apply the usual restriction that the cycles must be "simple cycles", that is, they are paths that start and end at the same vertex but otherwise never visit any vertex twice.
    version: lib3.0
    include: true
