#enter the graph
USE GRAPH financialGraph

CREATE OR REPLACE QUERY q1b (VERTWX<Account> name) SYNTAX v3 {
  vset = {name};
  print vset with_embedding;
}

#compile and install the query as a stored procedure
install query -single q1b

#run the query
run query q1b("Scott")
